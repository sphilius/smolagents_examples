# Hugging Face API Token (for HfApiModel, e.g., in smolagent_basic.py)
HF_TOKEN=""

# Anthropic API Key (for Claude models via LiteLLM, e.g., in smol_claude.py)
ANTHROPIC_API_KEY=""

# Google Gemini API Key (for Gemini models via LiteLLM, e.g., in smolagent_gemini.py)
GEMINI_API_KEY=""

# Jina AI API Key (for Jina AI tools, e.g., in smoltools/jinaai.py)
JINA_API_KEY=""

# OpenAI API Key (often used by LiteLLM or other tools if OpenAI models are invoked)
# While not explicitly os.getenv("OPENAI_API_KEY") in every script, LiteLLM might look for it.
OPENAI_API_KEY=""

# For Ollama (e.g., smol_ollama.py):
# Ollama typically runs locally and may not require an API key, or a placeholder can be used.
# The API base URL is configured directly in the script (e.g., http://localhost:11434).
# OLLAMA_API_KEY="ollama" # Example if your Ollama setup/LiteLLM expects one.
